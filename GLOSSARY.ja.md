# 用語集

レシピが説明なしに使っている用語をまとめました。各レシピの冒頭にも「ここで使う用語」の表があり、
そのレシピに必要な数語だけを載せてあります。ここはもう少し詳しい版で、別の場所で出会った用語を
引くための場所です。

クオンツ金融がはじめての方に向けて書いています。すでにこの分野をご存じなら、末尾の h5i-db の
項目だけ読めば足ります。

- [読む順番](#読む順番)
- [マーケットデータとマーケットマイクロストラクチャ](#マーケットデータとマーケットマイクロストラクチャ)
- [リターン・ボラティリティ・リスク](#リターンボラティリティリスク)
- [アルファ研究とバックテスト](#アルファ研究とバックテスト)
- [予測市場](#予測市場)
- [オプション・金利・その他の資産クラス](#オプション金利その他の資産クラス)
- [h5i-db とストレージ](#h5i-db-とストレージ)

## 読む順番

**クオンツがはじめての方**。まず 00/01、00/02、00/05 でデータベースをつかみ、次に 01/01（バー）、
01/04（約定と気配の結合）、02/01（最初のバックテスト）へ進んでください。この6本で、残りのレシピが
前提にしている語彙がそろいます。

**クオンツは知っていて h5i-db がはじめての方**。00/09 でクエリビルダを、00/05 でバージョン管理を
見てから、ご自分の資産クラスのレシピへ飛んでください。各レシピ末尾の「まとめ」を読めば、その先を
読む価値があるかどうか判断できます。

## マーケットデータとマーケットマイクロストラクチャ

マーケットマイクロストラクチャは、価格がどう形成されるかを、日次終値ではなく個々の注文や約定の
レベルで扱う分野です。

- **ティック（tick）**。マーケットデータフィード上の1イベントです。ふつうは約定か気配の更新を指します。
- **テープ（tape）**。約定を時刻順に並べたストリームです。銘柄の約定履歴そのものを指します。
- **プリント（print）**。テープ上の1件の約定です。「1万株のプリント」はその大きさの約定1件を指します。
- **約定（trade）**。成立した取引で、価格・数量・時刻を持ちます。
- **気配（quote）**。取引所やディーラーが今出している売買の意思表示で、ビッドとアスクで表されます。
- **ビッド（bid）**。買い手が今払ってもよい最も高い価格です。
- **アスク（ask、オファー）**。売り手が今受け入れる最も安い価格です。
- **ミッド（mid）**。ビッドとアスクの平均です。「価格」を1つの数で表したいときの標準的な代用値です。
- **スプレッド（spread）**。アスクからビッドを引いた値です。すぐに往復売買する費用であり、流動性を
  提供することへの市場の対価でもあります。
- **クォーテッド・スプレッド**。約定が起きる前の、表示されたままのスプレッドです。
- **実効スプレッド（effective spread）**。約定価格とその時点のミッドとの距離を2倍した値です。約定が
  実際に払った額を測ります。スプレッドの内側で約定すれば、クォーテッド・スプレッドより小さくなります。
- **実現スプレッド（realized spread）**。同じものを、約定の数分「後」のミッドに対して測った値です。
  流動性供給側の稼ぎと、価格が逆行して被る損失とを切り分けます。
- **板（order book）**。ある銘柄の未約定注文を価格順に並べたものです。
- **ベストプライス（top of book、L1）**。最良ビッドと最良アスク、およびそれぞれの数量です。最も入手
  しやすく、最も安いマーケットデータです。
- **L2（板の厚み、depth）**。各サイドの複数の価格帯と、そこに置かれている数量です。ベスト気配を超える
  数量を扱う主張には必ず必要になります。
- **キューポジション（queue position）**。同じ価格帯の待ち行列の中で自分の注文が何番目かを指します。
  同一価格では到着順に約定するので、そもそも約定するかどうかを左右します。復元するには板の全イベントが
  必要で、定期スナップショットでは足りません。
- **取引所（venue）**。1つの取引所や取引システムです。米国株は十数か所で同時に取引されています。
- **市場の分散（fragmentation）**。同じ銘柄が多数の取引所で同時に取引される状態です。どの1つの板も
  市場全体ではありません。
- **NBBO**。全米最良気配です。全取引所を通じた最も高いビッドと最も安いアスクを連続的に計算したもので、
  執行品質は規制上これを基準に測られます。
- **ロック（locked）**。取引所をまたいでビッドとアスクが等しい状態です。**クロス（crossed）** は
  ビッドがアスクを上回る状態を指します。どちらも市場の分散が生む一時的な現象です。
- **バー（bar、ローソク足）**。ティックを一定の時間区間に集計したものです。
- **OHLCV**。バーの5つの値、すなわち始値・高値・安値・終値・出来高です。
- **セッション（session）**。取引所が定める1営業日で、たとえば米国株ならニューヨーク時間 09:30〜16:00
  です。UTC の暦日とは違います。
- **DST（夏時間）**。取引所のセッションは現地の時計時刻で固定されているため、UTC からのオフセットが
  年2回ずれます。
- **VWAP**。出来高加重平均価格です。売買代金を出来高で割った値で、市場全体が得た平均価格なので、
  執行の標準ベンチマークになります。
- **TWAP**。時間加重平均価格で、出来高を無視します。出来高は必ず特定の時間帯に偏るため、VWAP とは
  必ず乖離します。
- **到着価格（arrival price）**。注文がデスクに渡った瞬間の価格です。これを基準にすると、約定だけでなく
  遅延の分もトレーダーの費用に数えられます。
- **スリッページ（slippage）**。想定した価格と実際に得た価格との差です。
- **インプリメンテーション・ショートフォール**。意思決定をポジションに変えるまでの総費用です。
  スリッページ、手数料、そして約定しなかった分も含みます。
- **ベーシスポイント（bp）**。1パーセントの100分の1、つまり 0.01% です。執行費用やスプレッドは小さい
  ので bp で表します。
- **マーケットインパクト**。自分が売買したこと自体によって価格が逆方向に動くことです。大口注文はこれを
  払うので、数量が無制限だと仮定するバックテストは嘘をつきます。
- **レイテンシ（latency）**。意思決定から取引所に届くまでの遅延です。判断の根拠にした板が消えている
  ほどには長い時間です。
- **メイカー／テイカー**。メイカーは指値を置いて待ちます。テイカーはスプレッドを越えて即座に約定します。
  取引所はふつう両者に別の手数料を課します。
- **フィル（fill）**。自分の注文に対する約定です。**部分約定** は数量の一部だけ、**約定率（fill ratio）**
  は実際に約定した割合を指します。
- **ハーフスプレッド**。ビッドとアスクの差の半分です。1単位あたりの発注コストであり、バックテストが
  課金できる最小の正直なコストです。
- **マーケットメイカー**。ビッドとオファーの両方を出し、方向を予測するのではなく、その差で稼ぐ参加者です。
- **在庫（インベントリ）**。片側を約定させられた結果として抱えるポジションです。この仕事が実際に負う
  リスクはこれです。
- **在庫スキュー**。在庫を減らす側の気配が有利になるように、両方の気配をずらすことです。リターンではなく
  約定率でリスク削減を買います。
- **逆選択（adverse selection）**。価格が自分に不利に動く直前に、まさにそのタイミングで約定させられる
  ことです。相手のほうが何かを知っていたということです。
- **マークアウト（markout）**。約定から一定時間後のミッドです。あとから見て良い約定だったときに正に
  なる符号で見ます。逆選択に値段をつける標準的な方法です。
- **アメンド（amend）**。キャンセルして出し直すのではなく、生きている注文の価格や数量をその場で変える
  ことです。レイテンシがあると、この違いが気配の有無を決めます。
- **符号付き出来高（signed volume）**。買い方が仕掛けたときを正、売り方が仕掛けたときを負とした出来高です。
- **買い仕掛け／売り仕掛け（buyer-initiated / seller-initiated）**。どちらのサイドがスプレッドを越えて
  約定を成立させたかです。フィードはふつう教えてくれないので推定します。
- **Lee-Ready 法**。その推定の標準的なルールです。約定価格をその時点のミッドと比べ、上なら買い仕掛け、
  下なら売り仕掛けとし、同値のときはティックテストに任せます。
- **ティックテスト（tick test）**。直前の約定価格と比べて符号を決める方法です。上がっていれば買い、
  下がっていれば売りとします。
- **オーダーフロー・インバランス（OFI）**。ある区間の買い仕掛け出来高から売り仕掛け出来高を引いた値です。
  短期のマイクロストラクチャ・シグナルの主力です。
- **マイクロプライス（microprice）**。両サイドの数量で加重したミッドです。ビッド側の数量がはるかに多い板
  ではミッドより上に来ます。素のミッドより、価格がこれからどちらへ動くかをよく表します。
- **インバランス（imbalance）**。ベスト気配における、アスク数量に対するビッド数量の比です。
- **ビッド・アスク・バウンス**。連続する約定がビッドとアスクを行き来する現象です。ボラティリティのように
  見えますが違います。これが見える速さでサンプリングすると、分散の推定値が必ず膨らみます。
- **マイクロストラクチャ・ノイズ**。その効果の総称です。観測される価格は真の価格に取引由来の歪みが
  乗ったもので、サンプリング間隔が短いほど歪みが支配的になります。
- **古い気配（stale quote）**。もはや市場の状態を表していないほど古い気配です。結合に**許容時間
  （tolerance）** を設けると、それより古い気配とは結合せずに拒否できます。
- **コーポレートアクション**。価格ではなく証券そのものに起きる変更です。株式分割、配当、合併などを
  指します。
- **株式分割（split）**。株数を倍にする措置で、たとえば 4:1 です。価格は同じ倍率で割られるため、過去の
  価格をすべて調整しないとリターンが壊れます。
- **調整後終値（adjusted close）**。その後のすべての分割と配当で調整した終値です。時系列で比較できますが、
  新しいコーポレートアクションが起きるたびに値が変わります。
- **調整係数（adjustment factor）**。生の価格を調整後の価格に変換する係数です。別に持っておくと、生の
  テープをそのまま残せます。

## リターン・ボラティリティ・リスク

- **リターン（return）**。ある期間の価格変化率です。**単純リターン** は `p_t / p_{t-1} - 1`、
  **対数リターン** は `ln(p_t / p_{t-1})` で、後者は時間方向に足し合わせられるので多期間の計算に向きます。
- **ボラティリティ（volatility、vol）**。リターンの標準偏差です。ポジションの危険度を表す既定の代理変数です。
- **年率化（annualized）**。1期間あたりの値を1年に換算することです。日次リターンは252倍、日次ボラティリティ
  は `sqrt(252)` 倍します。252は年間の営業日数です。
- **実現ボラティリティ（RV）**。観測されたリターンから測ったボラティリティで、ふつうは日中の二乗リターンの
  和の平方根です。モデルを置かないので、ノンパラメトリックと呼ばれます。
- **シグネチャプロット**。実現分散を、計算に使ったサンプリング間隔に対してプロットしたものです。速い
  サンプリングではビッド・アスク・バウンスで推定値が膨らむため右上がりになり、これを見て頻度を選びます。
- **バイパワー変動（bipower variation）**。「隣り合う」絶対リターンの積から作る分散推定量です。大きな
  ジャンプは2項にしか影響しないので、実現分散から引けばジャンプ成分を取り出せます。
- **ジャンプ（jump）**。連続的な拡散の一部とするには大きすぎる価格変化です。ニュースによる窓開けなどを
  指します。
- **EWMA**。指数加重移動平均です。新しい観測値に重み `alpha` を与え、それまでの推定値に `1 - alpha` を
  残すので、直近のデータほど重く、古いデータはなめらかに薄れていきます。
- **RiskMetrics のラムダ**。同じ漸化式を
  `sigma_t^2 = lambda * sigma_{t-1}^2 + (1 - lambda) * r_t^2` と書いたもので、日次では
  `lambda = 0.94` です。h5i-db の `alpha` は `1 - lambda` なので、0.94 は `ewma(x, 0.06)` になります。
- **重心（center of mass）・半減期**。EWMA がどれだけ過去を覚えているかの表し方です。日次の
  `lambda = 0.94` は重心にしておよそ16営業日にあたります。
- **ボラティリティ・ターゲティング**。実現リスクが一定の水準に近づくようポジションを調整し、ボラティリティが
  上がればエクスポージャを落とす手法です。まずリスクの形を整える道具であって、リターンが改善するのは
  副次的です。
- **レバレッジ（leverage）**。資本に対するエクスポージャの倍率です。2倍なら資本1ドルあたり2ドルの
  ポジションを持ちます。
- **グロス・エクスポージャ**。ロングとショートを符号なしで足した、ポジション全体の大きさです。
- **エクイティカーブ**。戦略の累積価値の推移です。ふつう1ドルの成長として描きます。
- **NAV（純資産価値）**。ある時点でのブック全体の価値です。
- **ドローダウン（drawdown）**。エクイティカーブの直近ピークからの下落率です。**最大ドローダウン** は
  期間中で最悪の値で、実際に戦略を止めさせるのはこの数字です。
- **シャープレシオ**。年率リターンを年率ボラティリティで割った値です。リスク1単位あたりの報酬で、
  この分野の既定の比較指標です。
- **P&L（損益）**。**実現損益** は決済済みの取引から確定した分、**含み損益** は未決済ポジションの
  評価上の損益です。
- **時価評価（mark to market）**。未決済ポジションを取得価格ではなく現在の市場価格で評価することです。
- **VaR（バリュー・アット・リスク）**。ある信頼水準のもとで、1日の損失がこれを超えないという水準です。
  たとえば「1日 99% VaR が 240万ドル」と表します。それを超えた日がどれほど悪いかは何も言いません。
- **期待ショートフォール（ES、CVaR）**。VaR を超えた日の損失の平均です。VaR がかわした問いに答えます。
- **ヒストリカル VaR**。過去リターンの経験分布からそのまま読む方法です。**パラメトリック VaR** は分布
  （ふつう正規分布）を仮定して分位点を読みます。
- **超過（exception、breach）**。損失が VaR の予測を超えた日です。99% VaR なら全体の約1%で起きるはずです。
- **Kupiec の POF 検定**。観測された超過回数が、掲げた信頼水準と整合するかを検定します。
- **ベータ（beta）**。市場が1単位動いたときに資産がどれだけ動くかです。資産リターンを市場リターンに回帰した
  ときの傾きにあたります。
- **マーケットモデル**。`r_asset = alpha + beta * r_market + e` という回帰で、市場全体の動きを取り除くために
  使います。
- **異常リターン（abnormal return）**。リターンからマーケットモデルの予測を引いた残りです。調べている
  イベントに帰属する部分にあたります。
- **CAR（累積異常リターン）**。異常リターンをイベント窓にわたって累積した値です。

## アルファ研究とバックテスト

- **アルファ（alpha）**。既知のリスク要因では説明できないリターンです。大まかには、成果のうち技量による
  部分を指します。
- **シグナル（signal）**。データから計算した、取るべきポジションを示す数値です。
- **ファクター（factor）**。多数の資産に共通してリターンを説明する特性です。バリュー、モメンタム、
  クオリティなどがあります。**ファクターライブラリ** は、それらを保存しバージョン管理したパネルです。
- **クロスセクション（cross-sectional）**。同じ時点で資産どうしを比べることです。**時系列（time-series）**
  は1つの資産をその過去と比べることを指します。
- **モメンタム（12-1）**。上がったものを買う戦略です。標準形は直近12か月のリターンから直近1か月を除いた
  ものを使います。その1か月は反転しやすいからです。
- **バリュー（B/P）**。純資産を株価で割った値です。高いほど、会計上の価値に比べて割安ということになります。
- **クオリティ（quality）**。収益性や安定性を表す指標群です。このクックブックでは売上成長の安定度を代理に
  使っています。
- **パネル（panel）**。1行が1資産1日付になる、2次元のデータセットです。
- **リバランス（rebalance）**。目標ウェイトを計算し直し、そこへ向けて売買することです。定期的に行います。
- **回転率（turnover）**。1回のリバランスでブックのどれだけを売買するかです。取引費用を直接決めます。
- **情報係数（IC）**。シグナルと、その後に実現したリターンとの相関です。月次 IC が 0.03〜0.05 あれば株式
  ファクターとして本物なので、期待値は低めに置いてください。
- **五分位スプレッド（quintile spread）**。シグナルで資産を5つに分け、上位から下位のリターンを引いた値です。
  シグナルが勝者と敗者を分けられているかを、モデルなしで読み取れます。
- **共和分（cointegration）**。個々には彷徨うのに、その差は彷徨わない2つの価格系列です。ペアトレードの
  統計的な根拠になります。
- **Engle-Granger 検定**。その標準的な2段階検定です。一方を他方に回帰し、残差が定常かどうかを調べます。
- **ヘッジレシオ**。第1レグ1単位に対して第2レグを何単位持つかです。共通の動きに対してペアが中立になるよう
  決めます。
- **スプレッド（ペアトレードの）**。そのヘッジ済み組み合わせの残差です。上のビッド・アスク・スプレッドとは
  別の意味です。
- **Zスコア**。ある値がローリング平均から標準偏差いくつ分離れているかです。スプレッドを比較可能な売買
  トリガーに変換します。
- **平均回帰（mean reversion）**。平均へ戻ろうとする性質です。その**半減期** は、乖離が半分に減衰するまでの
  時間を指します。
- **バックテスト**。過去データで戦略をシミュレーションすることです。
- **イベント駆動バックテスト**。バーの価格で約定したと仮定するのではなく、注文が記録されたマーケットデータに
  1イベントずつぶつかる形式のバックテストです。
- **先読みバイアス（lookahead bias）**。その時点では手に入らなかった情報を使ってしまうことです。最も
  ありふれ、最も成績を良く見せるバグです。
- **ポイントインタイム（point-in-time）**。各時点で「知られていたとおり」にデータを保存し、参照することです。
  先読みバイアスを避けられるのはこれがあるからです。
- **報告ラグ（reporting lag）**。財務数値が対象とする期間と、公表された日付との差です。ふつう25〜55日あります。
- **生存バイアス（survivorship bias）**。今も存在する資産だけで検証してしまうことです。失敗した銘柄が
  そっと標本から消えます。
- **リステートメント（restatement）**。ベンダーが後から過去データを訂正することです。同じバックテストを
  再実行したときに見えるものが変わります。
- **インサンプル／アウトオブサンプル**。戦略を作るのに使ったデータと、検証のために取っておいたデータです。
- **ホールドアウト（holdout）**。最後まで手をつけずに取っておくアウトオブサンプルのデータです。希少である
  ことが価値なので、使えるのは一度きりです。
- **ウォークフォワード**。ある窓で推定し、その次の窓で検証することを、前へ転がしながら繰り返します。実際の
  運用に近い検証です。
- **オーバーフィット（overfitting）**。再現する構造ではなく、手元の標本のノイズを説明する規則を見つけて
  しまうことです。
- **多重検定（multiple testing）**。多くの候補を試すほど、勝者が運で良く見えます。試した数が多いほど、
  勝者が越えるべき基準は高くなります。
- **PBO（バックテスト過学習確率）**。試行集合を繰り返し分割し、インサンプルの勝者がアウトオブサンプルで
  中央値を下回る頻度を測ります。0.5 を超えるなら、その選び方はランダムより悪いということです。
- **デフレーテッド・シャープレシオ**。試した候補の数に加えて、リターンの歪度と尖度でシャープレシオを下方
  修正した値です。表向きの数字が探索の規模に耐えるかを答えます。
- **最小トラックレコード長**。主張したシャープレシオがゼロと統計的に区別できるまでに、どれだけの運用期間が
  必要かを示します。
- **エンバーゴ（embargo）**。学習データと検証データの間に空ける間隔です。窓が重なることで境界をまたいで
  情報が漏れるのを防ぎます。
- **イベントスタディ**。日付のついたイベントの前後の窓で異常リターンを測る手法です。**推定窓** で
  マーケットモデルを推定し、**イベント窓** で効果を測ります。
- **リードラグ（lead-lag）**。ある銘柄の動きが、別の銘柄の動きに系統的に先行することです。
- **相互相関関数（CCF）**。2つの系列を、さまざまな時間ずれで相関させたものです。ピークのずれが先行幅の
  推定値になります。
- **Epps 効果**。サンプリング間隔を短くするほど、測った相関がゼロへ向かって落ちる現象です。非同期な取引が
  生む見かけ上のもので、本当に相関が消えているわけではありません。
- **Hayashi-Yoshida 推定量**。重なり合うティック区間を直接組にする共分散推定量です。時間グリッドが要らない
  ので Epps 効果を避けられます。
- **季節性（seasonality）**。時刻・曜日・季節で繰り返すパターンです。日中の出来高は寄り引けが厚いU字を
  描きます。
- **取引費用**。意思決定時の価格より約定価格を悪くするものすべてです。手数料、取引所費用、スプレッド、
  マーケットインパクトが含まれます。
- **コミッション（commission）**。ブローカーや取引所が明示的に課す1取引あたりの手数料です。
- **ベクトル化バックテスト**。板を介さず、シグナル×その後のリターンとして損益を計算する方式です。
  速く、そして執行については何も語りません。
- **目標ポジション（target position）**。戦略が持ちたい保有量で、そこへ到達する注文とは別物です。
  一方を他方に変換するところが、リサーチのベクトルが注文意図になる場所です。
- **形成日（formation date）**。シグナルを作ったデータの日付です。その注文が市場に届けられる日付とは
  区別します。
- **突き合わせ（reconciliation）**。同じものの2つの計算の差を、残差がゼロになるか名前がつくまで
  説明しきることです。
- **親注文とスライス**。親注文はデスクが売買を依頼された全数量、スライスはそれを執行するための子注文です。
- **到着価格（arrival price）**。判断した瞬間のミッドです。あとから売買しても有利にできないベンチマークです。
- **POV（出来高参加率）**。プリントした出来高の一定割合を売買する方式です。時計ではなく相場に合わせます。
- **機会コスト**。アルゴリズムが買えなかった株数を、最後に買っていたら払ったはずの金額です。これを
  外すと、売買しないアルゴリズムが得をします。
- **参加率（participation rate）**。最良気配に表示されていた数量に対する注文サイズの割合です。コスト
  モデルの自然な横軸になります。
- **平方根則**。マーケットインパクトの実証的な形です。コストはサイズに比例するのではなく、およそ
  平方根で増えます。
- **時系列モメンタム**。自分の過去より上がっている資産をロング、下がっている資産をショートする方式です。
  資産どうしの比較はしません。
- **ボラティリティ・スケーリング**。各ポジションを自分のボラティリティに反比例させ、どの資産も同程度の
  リスクを出すようにすることです。ポートフォリオ全体で狙う水準が **ボラティリティ目標** です。
- **クライシスアルファ**。相場が最悪の局面でこそトレンドフォローは報われる、という主張です。月次の
  主張であり、日次版はたいてい検証に耐えません。
- **プラトー（平坦域）**。1点だけでなく、その周辺のパラメータでも同じように機能する範囲です。パラメータが
  当てはめではなく選ばれたことを示す、いちばん弱い証拠です。
- **ドルニュートラル**。ロングとショートを同額にし、相場全体の方向を損益から打ち消すことです。
- **ノートレード・バンド**。目標の変化が売買代金に見合わないうちはポジションを動かさないことです。
  回転率を動かす標準的なレバーです。
- **損益分岐コスト**。純リターンがちょうどゼロになる売買コストです。速い戦略ではこれがすべてを決めます。
- **必要グロスリターン**。回転率×コストです。シグナルの良し悪しを語る前に超えるべき線です。
- **先行リターン（forward return）**。これから n バーぶんのリターンで、ファクターを採点する相手です。
- **分位バケット**。日付ごとにファクター値で等件数に分けたグループです。上位から下位を引いたものが
  **分位スプレッド** です。
- **順位の自己相関**。銘柄のファクター順位が次の日付までどれだけ保たれるかです。低ければ、ポートフォリオを
  組む前から回転率が高いということです。
- **グループニュートラル**。同じセクター内の銘柄とだけ比べることです。セクターの賭けを検証可能な主張に
  変えます。
- **ラベルと期間（ホライズン）**。ラベルは観測を採点する相手となる結果で、期間はそれが何観測ぶん先まで
  届くかです。
- **パージ（purging）**。ラベルがテストブロックに重なる学習行を落とすことです。エンバーゴと合わせて、
  時系列で交差検証を正当にします。
- **CPCV**。組み合わせパージド交差検証です。ブロックのあらゆる組み合わせでテストするので、出力は1つの
  数字ではなく分布になります。
- **グリッドサーチ／ランダムサーチ**。パラメータの組み合わせを列挙するか、範囲から引くかの違いです。
  空間が広く、ほとんどの軸が効かないときはランダムサーチが勝ちます。
- **試行台帳（trial ledger）**。採点した設定を、何を読み何をしたかで記録したものです。過剰適合の補正が
  必要とする試行回数を、主張ではなく計数で得られます。
- **台帳リプレイ**。口座の公開された売買を注文意図にコンパイルし、当時の板に受理か拒否かを決めさせる
  ことです。拒否した場所が発見になります。
- **IOC（即時執行・残数量取消）**。いま取れるだけ約定させ、残りは取り消す条件です。
- **reduce-only**。ポジションを減らすことだけができ、反対側を新規に建てられない注文です。
- **リターン系列**。1期間につき1つの単純（非累積）リターンです。すべてのパフォーマンス統計の入力です。
- **年率換算係数**。1年が何期間かです。日次バーなら252、月次なら12、時間足の暗号資産なら 24×365 です。
- **ソルティノレシオ**。シャープと同じ考え方で、下振れのボラティリティだけで割ったものです。
- **カルマーレシオ**。年率リターンを最大ドローダウンで割ったものです。
- **ティアシート**。見出しの統計量、累積リターン、ドローダウン、ローリングのリスクを1ページにまとめた
  標準的なパフォーマンスレポートです。
- **来歴（provenance）**。その数字を生んだピン、パラメータ、クエリです。**ダイジェスト** としてハッシュ
  されるので、2つの結果を厳密に比べられます。

## 予測市場

イベント契約とも呼ばれます。指定した出来事が起きれば 1.00、起きなければ 0.00 を支払う契約なので、価格が
そのまま確率として読めます。

- **バイナリ契約**。結果が2つの場合です。**YES** は出来事が起きたときに、**NO** は起きなかったときに
  支払われます。
- **決着（resolution）**。どちらの結果になったかの確定です。
- **決済（settlement）**。その確定に基づく支払いです。決済まで持ったポジションの価値はちょうど 1.00 か
  0.00 になります。
- **満期（expiration）**。取引が止まる時点です。決着はそれより後に来ることがあり、その差が問題になります。
- **観測可能性（observability）**。結果が実際に知り得るようになった時点です。バックテストが決着を使って
  よいのはこの時点より後であって、出来事が起きた瞬間ではありません。
- **決済ラグ**。満期から、結果が観測可能になるまでの遅れです。
- **パリティ（parity）**。ちょうど一方だけが支払われるので、YES と NO は 1.00 に足し合わなければなりません。
- **ベーシス（basis）**。その差の大きさです。「2セントのベーシス」なら、ペアの合計が 0.98 という意味です。
- **手数料カーブ**。予測市場の手数料はふつう `p * (1 - p)` に比例するので、0.50 で最大、両端でほぼゼロに
  なります。投じた資本に対する割合で見ると `rate * (1-p)` となり、契約が高くなるほど下がります。
- **フェイバリット・ロングショット・バイアス**。イベント市場で最も古くから知られている歪みです。安い契約
  （**ロングショット**）は系統的に割高で、高い契約（**フェイバリット**）は割安になります。
- **キャリブレーション（calibration）**。提示された確率が実際の頻度と一致しているかです。完全に整合していれば、
  0.30 の値がついた出来事は 30% の頻度で起きます。
- **信頼性曲線（reliability curve）**。提示確率に対して実測頻度をプロットしたものです。対角線が完全な
  キャリブレーションを表します。
- **Brier スコア**。確率予測の平均二乗誤差で、小さいほど良い指標です。**reliability**（キャリブレーション
  誤差）、**resolution**（予測が基準率からどれだけ離れるか。大きいほど良い）、**uncertainty**（予測者
  ではなく出来事の性質）の3つに分解できます。
- **対数損失（log loss）**。別の評価指標で、自信を持った誤りを Brier スコアよりはるかに強く罰します。
- **基準率（base rate）**。その出来事が全体として起きる頻度です。どんな予測もまずこれを超える必要があります。
- **UMA**。Polymarket が結果の確定に使う分散型オラクルのプロトコルです。

## オプション・金利・その他の資産クラス

- **インプライドボラティリティ（IV）**。価格モデルに入れるとオプションの市場価格が再現される
  ボラティリティです。市場の予測を、トレーダーが比較できる1つの数値として表したものです。
- **IV サーフェス**。権利行使価格と満期にわたるインプライドボラティリティを、1つの面として捉えたものです。
- **スマイル／スキュー**。同じ満期における権利行使価格方向の IV の形です。株価指数オプションはスキューして
  おり、下方の行使価格ほど高いボラティリティがつきます。
- **ターム構造**。同じ行使価格（ふつう ATM）における満期方向の形です。
- **ATM**。アット・ザ・マネー、つまり現在価格に近い行使価格です。
- **デルタ（delta）**。原資産が1単位動いたときのオプション価格の変化です。「25デルタ」のように行使価格の
  目印としても使われます。
- **リスクリバーサル**。同じデルタのコールとプットの IV の差です。スキューを1つの数で表します。
- **バタフライ**。両翼と ATM の IV の差です。スマイルの曲がり具合を1つの数で表します。
- **満期（expiry）**。オプション契約が満期を迎える日付です。**テナー** は残存期間なので、保存せずに
  導出すべき値です。
- **チェーン（chain）**。ある時点における、1つの原資産の上場オプション全部です。
- **イールドカーブ**。満期に対して金利をプロットしたものです。**パーカーブ** は、債券価格が100になる
  クーポンとして表した曲線です。
- **テナー（金利の）**。カーブ上の満期の点です。2年、10年などを指します。
- **傾き（slope）**。長いテナーの利回りから短いテナーの利回りを引いた値です。10年マイナス2年などです。
- **曲率（curvature）**。カーブの中央を両端と比べた値です。バタフライとも呼ばれます。
- **キャリー（carry）**。カーブが動かないとして、時間が経つだけで得られる収益です。
- **ロールダウン（rolldown）**。右上がりのカーブ上で債券が時間とともに低い点へ移ることで得られる利益です。
- **マーク（mark）**。ポジションの公式価格です。1日1回、あるいはもっと頻繁に付けます。

## h5i-db とストレージ

- **組み込みデータベース**。自分のプロセスの中で動き、サーバを立てる必要がないデータベースです。SQLite や
  DuckDB と同じく、データベースはディスク上の1ディレクトリです。
- **Arrow**。h5i-db が話すインメモリの列指向フォーマットです。pandas、polars、pyarrow はいずれも安く
  変換できます。
- **Parquet**。ディスク上の列指向ファイル形式です。列指向なので、3列だけ触るクエリはその3列しか読みません。
- **スキーマ（schema）**。テーブルの列名と型で、作成時に固定されます。
- **時刻列（time column）**。h5i-db がソートと枝刈りに使う列です。すべてのテーブルが1つ宣言します。
- **ソートキー（sort key）**。セグメント内の物理的な行順です。時刻列で始める必要があり、結合や集計が
  どれだけストリーム処理できるかを決めます。
- **コミット（commit）**。1回のアトミックな書き込みです。`append`、`write`、`delete`、`restore` の
  いずれもコミットを生みます。
- **バージョン（version）**。あるコミット後のテーブルの状態です。イミュータブルで、いつまでも読めます。
- **マニフェスト（manifest）**。あるバージョンがどのセグメントで構成されるかを記した小さなファイルです。
  古いバージョンの読み出しは、ログの再生ではなくマニフェストの参照で済みます。
- **セグメント（segment）**。ある時間範囲の行を持つ、イミュータブルな Parquet ファイル1つです。
- **タイムトラベル**。過去のバージョン、実時刻（`as_of`）、名前付きスナップショットのいずれかで、当時の
  テーブルを読むことです。SQL では `h5i('trades', 7)` と書きます。
- **スナップショット（snapshot）**。1つ以上のテーブルバージョンに名前とチェックサムを付けて固定したものです。
  データを複製せずマニフェストを参照するだけなので O(1) です。
- **ピン留め（pin）**。実行の入力を特定のバージョンやスナップショットに固定することです。再実行が同じ
  バイト列を読みます。
- **append と write**。`append` は末尾に行を足します。`write` は内容全体を差し替えますが、新しい
  バージョンとして書くので、古いバージョンはそのまま読めます。
- **楽観的並行制御**。書き手はロックを取りません。コミットは自分が拡張するつもりだったバージョンを持ち、
  ヘッドが動いていれば拒否されます。
- **`ConflictError`**。その拒否です。更新の消失ではなく、リトライすべき事象です。
- **plan / apply**。削除や置換はまずプランとして積まれ、行数と前後のサンプルを添えて提示されます。検分して
  から適用するとコミットになります。Python API で削除できる経路はこれだけです。
- **変更ポリシー（mutation policy）**。破壊的な直接書き込みを禁じるデータベース単位の設定です。検分つきの
  plan / apply だけが通ります。
- **`compact`**。小さなセグメント群をより大きく少数のセグメントにまとめ、新しいバージョンとして書きます。
  必要になるのはストリーミング取り込みのせいです。
- **`vacuum`**。参照されていないストレージを回収します。コミット済みのバージョン履歴を消すことはありません。
- **`verify`**。チェックサムの連鎖をたどります。`verify(deep=True)` は保存された全バイトを再計算します。
- **枝刈り（pruning）**。クエリの条件に合いようがない時間範囲のセグメントを、I/O の前にまるごと飛ばすことです。
- **プロジェクション（projection）**。クエリが必要とする列だけを読むことです。
- **DataFusion**。h5i-db がクエリに使う Apache の SQL エンジンです。フル SQL を提供し、時系列演算子は
  h5i-db が足しています。
- **DataFrame ビルダ**。`db.table(...)` と動詞をつないだ、Python の値として持てる遅延クエリです。SQL に
  コンパイルされ、`.sql()` で生成物が見えます。
- **遅延（lazy）**。`.collect()` のような終端呼び出しまで、何も実行されません。
- **`time_bucket`**。タイムスタンプをグリッドに丸めます。タイムゾーン名を渡せば、DST を含めて現地の
  時計時刻にバケット境界がそろいます。
- **ウィンドウ関数**。現在行に関係する行の集まりにまたがって計算する SQL で、行はまとめられません。
  `lag()` やローリング平均などがあります。
- **CTE**。`WITH` で導入する名前付きの副問い合わせです。多段のクエリを読みやすく保てます。
- **ASOF 結合**。左の各行を、そのタイムスタンプ以前で最も新しい右の行に結合します。約定に気配を、価格に
  財務数値を、ポジションにマークを貼り付ける操作の正体です。
- **gapfill / resample**。不規則な系列を規則的なグリッドに載せます。`'null'` は穴を見えるまま残し、
  `'locf'` は直前の観測値を持ち越し、`'interpolate'` は前後を直線で結びます。
- **locf**。直前の観測値の持ち越しです。手軽ですが危険で、同じ価格が繰り返されると、誰も取引できなかった
  ゼロリターンが並びます。
- **`tail`**。指定したバージョンより後にコミットされた行だけを読みます。バージョン管理されたテーブルは
  メッセージログにもなり、タイムスタンプをカーソル代わりにする当て推量が要りません。
- **ハイウォーターマーク**。消費側が処理し終えた最後のバージョンです。再開できるよう保存しておきます。
- **`arrival_delta`**。同じクエリを現在のヘッドと過去の意思決定時点の両方で実行し、その差を報告します。
  動いた分が、当時まだ届いていなかったデータに依存していた部分です。
- **実行フォーク（run fork）**。バックテストが注文・約定・ポジション・エクイティを書き込む、隔離された
  データベースの枝です。実行どうしがぶつかりません。
- **プリフライト（preflight）**。対応できない要求や危険な要求を、エンジンに届く前に拒否する検査です。
  スナップショットしかないデータでキューポジションを主張する場合などが該当します。
- **スイープのフォーク**。同じ考え方をひとつ上の層で使ったものです。`quant.sweep` はパラメータグリッドの
  試行ごとにコピーオンライトの枝を与えるので、試行どうしが汚染し合わず、1つのクエリで全部を比較できます。
- **バスケットレポート**。多数の実行の保存済みテーブルから組み立てた、自己完結の1つの文書です。再
  シミュレーションはせず、描かなかったパネルは黙って間引かずに記録します。
